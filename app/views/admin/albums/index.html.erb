<section class="container section-container order-section">
  <h1>Albums Info</h1>

  <table class="admin-albums">
    <thead>
      <tr>
        <th class="ljust">Album</th>
        <th>Title</th>
        <th>Description</th>
        <th>Artist</th>
        <th>Status</th>
        <th>Edit</th>
      </tr>
    </thead>
    <tbody>
      <% @albums.each do |album| %>
        <tr>
          <td class="ljust"><%= link_to image_tag(album.image_url), album %></td>
          <td><%= link_to album.title, album_path(album) %></td>
          <td><%= album.description %></td>
          <td><%= link_to album.artist.name, album.artist %></td>
          <td><%= album.status %></td>
          <td><%= link_to "Edit", edit_admin_album_path(album) %></td>
        </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="4"><%= link_to "New Album", new_admin_album_path %></td>
      </tr>
    </tfoot>
  </table>
</section>
